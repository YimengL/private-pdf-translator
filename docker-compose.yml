services:
  ollama:
    image: ollama/ollama
    volumes:
      - ollama_data:/root/.ollama

  pipeline:
    build: .
    environment:
      - ANTHROPIC_API_KEY
      - OLLAMA_HOST=http://ollama:11434
    depends_on:
      - ollama

volumes:
  ollama_data:
